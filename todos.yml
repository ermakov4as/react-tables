info: to start server, 'nodemon run src/server.js'
В консоли: store.getState(); // при window.store = store;
Производительность: a) (shouldComponentUpdate/PureComponent)/(mose(params)/mose), b) reselect - current selectors

TODOs:
!?) RT?

1) Покомпонентный перевод на функциональные блоки
